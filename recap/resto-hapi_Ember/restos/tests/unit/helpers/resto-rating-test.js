import { restoRating } from 'restos/helpers/resto-rating';
import { module, test } from 'qunit';

module('Unit | Helper | resto rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restoRating([42]);
  assert.ok(result);
});
